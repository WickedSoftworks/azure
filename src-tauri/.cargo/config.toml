# ts-rs writes the generated TypeScript here when the tests run. Relative to
# this workspace root, so `cargo test` puts the bindings where the frontend
# imports them from no matter which directory it was invoked in.
[env]
TS_RS_EXPORT_DIR = { value = "../src/lib/bindings", relative = true }

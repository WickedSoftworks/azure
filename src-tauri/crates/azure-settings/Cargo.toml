[package]
name = "azure-settings"
version = "0.1.0"
edition = "2021"
description = "Hotkey bindings, their rules, and where settings are kept."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ts-rs = "12"
